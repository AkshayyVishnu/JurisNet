# JurisNet runtime dependencies — Python 3.10–3.13.
# Install:  pip install -r requirements.txt

# Config / env
python-dotenv>=1.2.1
numpy>=1.26

# Chunking
beautifulsoup4>=4.14
lxml>=5.2

# Embeddings — voyage-4-large (document) + voyage query embedding, via API
voyageai>=0.4.1

# Vector store
qdrant-client>=1.18

# Graph store
neo4j>=6.2

# LLM gateway — ALL agent + enrichment calls route through LiteLLM, which talks to
# Cerebras / Groq / Gemini over REST (no per-provider SDK required).
litellm>=1.89

# SQLite FTS5 ships with CPython's stdlib sqlite3 — no package needed.

# Testing
pytest>=9.0

# ── Optional ──
# Only needed for local voyage-4-nano query mode (QUERY_EMBED_MODE=nano).
# The default "large" query mode uses the Voyage API and does NOT need these.
# sentence-transformers>=3.0
# torch>=2.2
